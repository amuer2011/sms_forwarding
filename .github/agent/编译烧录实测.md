# 编译烧录实测

> 板型: **MakerGO ESP32 C3 SuperMini** | FQBN: `esp32:esp32:makergo_c3_supermini`  
> 串口: **COM4** | 项目: `D:\dev\sms_forwarding\code`

## 2026-08-03 网络恢复改造验证

已完成：

- `network_recovery_core` 主机测试通过，编译参数为 `-std=c++17 -Wall -Wextra -pedantic`。
- 已覆盖 CEREG 结构化解析、当前 PLMN 解析、真实 `COPS=?` 响应排序、forbidden 候选保留、last-good 失败阈值、串口所有权和跨循环行组装。
- `network_recovery.cpp`、`operator_cache.cpp`、`modem.cpp` 使用轻量 Arduino API 桩完成语法检查，无警告。
- 静态检查确认没有分钟级阻塞 `delay` 或循环扫描 `COPS`。
- GitHub Actions 固定 ESP32 Core `3.3.10`，同时编译 `esp32c3` 和 `makergo_c3_supermini`。

本机未完成：

- 完整 Arduino 编译。便携版 `arduino-cli` 已可运行，但 Espressif RISC-V 工具链下载多次中断，ESP32 Core 未完成安装。
- 因此本次改造后的 Flash/RAM 数值尚未取得。改造前记录为 Flash `1,261,557 / 1,310,720` 字节、RAM `41,256 / 327,680` 字节。
- 硬件烧录与真实 ML307R 场景验证尚未执行，不能标记为通过。

烧录后必须验证：正常自动注册、last-good 恢复、无缓存扫描、forbidden 联通成功、全部失败退避、恢复期间普通网页响应、模组接口 429、恢复中短信到达、冷启动重新自动选网、换卡不复用缓存。

## 前置条件

- arduino-cli 已安装（位于 `C:\Program Files\Arduino IDE\resources\app\lib\backend\resources\arduino-cli.exe`）

## 一条命令编译

```powershell
$env:Path = "C:\Program Files\Arduino IDE\resources\app\lib\backend\resources;$env:Path"
$env:ARDUINO_DIRECTORIES_DATA = "D:\dev\arduino_pack"
$env:ARDUINO_DIRECTORIES_USER = "D:\dev\arduino_pack\user"
arduino-cli compile --fqbn esp32:esp32:makergo_c3_supermini --build-path "D:\dev\arduino_pack\build" "D:\dev\sms_forwarding\code"
```

> 首次编译约 3-5 分钟，耐心等待，不要提早结束。

## 一条命令烧录

```powershell
arduino-cli upload --fqbn esp32:esp32:makergo_c3_supermini --port COM4 --input-dir "D:\dev\arduino_pack\build" "D:\dev\sms_forwarding\code"
```

## 查看日志

```powershell
arduino-cli monitor --port COM4 --config 115200
```
